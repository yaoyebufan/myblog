package com.sunyue.myblog.commons;

import java.io.File;

public class FileUtil {
    public static void delete_file(String path){
        /*path = "C:\\my_cake\\my_cake\\my_cake_web\\src\\main\\webapp"+path;
        System.out.println(path);
        File file = new File(path);
        if(!file.exists()) {
            System.out.println("不存在此文件");
        };
        if(file.isFile() || file.list()==null) {
            file.delete();
            System.out.println("删除了"+file.getName());
        }*/
    }
}
